<h2>
  {{model.id}} ({{model.currentVersion.name}})
</h2>
Versions:
<ul>
  {{#each model.sortedVersions as |version|}}
    <li>{{version.name}}
      Deps:
      <ul>
        {{#each version.dependencies as |dep|}}
          <li>{{dep.id}}</li> 
        {{/each}}
      </ul>
    </li>
  {{/each}}
</ul>
